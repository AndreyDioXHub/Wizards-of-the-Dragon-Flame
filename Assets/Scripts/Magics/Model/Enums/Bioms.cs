using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace com.czeeep.spell.biom {
    public enum Bioms {
        none = 0,
        life = 1,
        fire = 2,
        water = 4,
        freeze = 16,
        dark = 64
    }
}